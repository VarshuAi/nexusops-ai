version: '3.8'

services:
  nexusops-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexusops-ai
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
